first_name = "Reiko"
last_name = "Matsuki"

def password_generator(f,l):
  temp = f[len(f) - 3:] + l[len(l) - 3:]
  return temp

temp_password = password_generator(first_name,last_name)
print(temp_password)
